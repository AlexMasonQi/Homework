#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int trimSpace(char *inbuf, char *outbuf)
{
	int ret = 0;
	if (inbuf == NULL)
	{
		ret = -1;
		printf("The func trimSpace() err:%d\n", ret);
		exit(0);
	}
}

int main()
{

	system("pause");
	return 0;
}